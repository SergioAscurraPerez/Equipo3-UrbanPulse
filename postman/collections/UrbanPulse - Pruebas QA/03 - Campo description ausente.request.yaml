$kind: http-request
url: http://localhost:5678/webhook/urbanpulse/report
method: POST
body:
  type: json
  content: '{ "latitude": -12.0464, "longitude": -77.0428 }'
scripts:
  - type: afterResponse
    code: pm.test("Devuelve error de descripcion obligatoria", () => { const json = pm.response.json(); pm.expect(json.error).to.include("obligatoria"); });
    language: text/javascript
order: 3850787020851970
