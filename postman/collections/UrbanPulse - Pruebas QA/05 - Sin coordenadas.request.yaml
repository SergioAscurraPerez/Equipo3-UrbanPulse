$kind: http-request
url: http://localhost:5678/webhook/urbanpulse/report
method: POST
body:
  type: json
  content: '{ "description": "Hay un incidente" }'
scripts:
  - type: afterResponse
    code: pm.test("Responde 200 y persiste el reporte sin coordenadas (latitude/longitude nulos)", () => { pm.response.to.have.status(200); const json = pm.response.json(); pm.expect(json.latitude).to.be.null; pm.expect(json.longitude).to.be.null; });
    language: text/javascript
order: 4489082766723460
